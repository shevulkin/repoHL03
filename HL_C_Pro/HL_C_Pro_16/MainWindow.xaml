<Window x:Class="MyApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Data Binding з Командами" Height="320" Width="450">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="80"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Ім'я -->
        <TextBlock Grid.Row="0" Grid.Column="0"
                   Text="Ім'я:" VerticalAlignment="Center" Margin="0,0,10,10"/>
        <TextBox   Grid.Row="0" Grid.Column="1"
                   Margin="0,0,0,10"
                   Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"/>

        <!-- Email -->
        <TextBlock Grid.Row="1" Grid.Column="0"
                   Text="Email:" VerticalAlignment="Center" Margin="0,0,10,10"/>
        <TextBox   Grid.Row="1" Grid.Column="1"
                   Margin="0,0,0,10"
                   Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}"/>

        <!-- Результат — оновлюється сам! -->
        <TextBlock Grid.Row="2" Grid.Column="0"
                   Grid.ColumnSpan="2"
                   Text="{Binding Greeting}"
                   FontSize="16"
                   Foreground="Green"
                   Margin="0,10,0,0"/>

        <!-- Лічильник з командами -->
        <StackPanel Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2"
                    Orientation="Vertical" Margin="0,15,0,0">
            
            <TextBlock Text="{Binding ClickCount, StringFormat='Натиснуто: {0}'}"
                       FontSize="14" Margin="0,0,0,10"/>
            
            <!-- Приклад команд -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                <Button Content="Збільшити (Command)"
                        Width="150"
                        Command="{Binding IncrementCommand}"
                        Margin="0,0,10,0"/>
                
                <Button Content="Скинути"
                        Width="100"
                        Command="{Binding ResetCommand}"
                        Margin="0,0,10,0"/>
            </StackPanel>
        </StackPanel>

        <!-- Кнопка очистки форми -->
        <Button Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="2"
                Content="Очистити форму"
                Command="{Binding ClearFormCommand}"
                Width="150"
                HorizontalAlignment="Left"
                VerticalAlignment="Top"
                Margin="0,10,0,0"/>

    </Grid>
</Window>